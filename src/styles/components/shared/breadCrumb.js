import { makeStyles } from "@mui/styles";
const useStyles = makeStyles((theme) => ({
  menuBox: {
    borderRadius: 10,
    display: "flex",
    flexDirection: "row",
  },
}));
export default useStyles;
